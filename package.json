{
  "name": "hymnsrepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build.prod": "JEKYLL_ENV=production bundle exec jekyll build -d ./dist"
  },
  "devDependencies": {
    "gulp": "3.9.1",
    "sw-precache": "^5.2.0"
  }
}
